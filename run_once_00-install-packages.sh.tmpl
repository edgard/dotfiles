{{ if eq .chezmoi.os "linux" -}}
#!/usr/bin/env bash
sudo apt update
sudo apt install -y build-essential python python3 python3-pip python3-venv git curl unzip
{{ else if eq .chezmoi.os "darwin" -}}
#!/usr/bin/env bash
brew update
brew install git
{{ end -}}
