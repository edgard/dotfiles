{{ if eq .chezmoi.os "linux" -}}
#!/usr/bin/env bash
sudo apt install -y build-essential python python3 python3-venv git curl unzip zsh
{{ else if eq .chezmoi.os "darwin" -}}
#!/usr/bin/env bash
brew install git
{{ end -}}
