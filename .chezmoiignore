README.md
LICENSE
.gitignore

{{ if ne .chezmoi.os "darwin" }}
# Ignore macOS-specific files on non-macOS systems
extras/config-osx.sh
extras/Brewfile.darwin
{{ end }}

{{ if ne .chezmoi.os "linux" }}
# Ignore Linux-specific files on non-Linux systems
extras/config-linux.sh
extras/Brewfile.linux
{{ end }}

# Always ignore the extras directory itself, but not its contents
# This prevents the directory from being created in the home directory
extras/
